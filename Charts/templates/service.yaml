apiVersion: v1
kind: Service
metadata:
  name: {{ include "springbootdemo.fullname" . }}
  labels:
    app: {{ include "springbootdemo.name" . }}
spec:
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
  selector:
    app: {{ include "springbootdemo.name" . }}
  type: {{ .Values.service.type }}